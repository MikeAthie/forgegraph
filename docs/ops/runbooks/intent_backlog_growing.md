# Intent Backlog Growing

Use `/api/metrics/slo` and `/api/operator/runtime-intents/backlog` to confirm backlog, lag, consumer idle time, and dead-letter growth. Recovery decisions remain backend-owned.
