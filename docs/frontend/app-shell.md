# App Shell

The app shell uses:

- left sidebar for domains
- top bar for organization scope and time framing
- main content for summary-first views
- secondary drill-downs through existing detail pages

This shell replaces the old header-first builder navigation.
