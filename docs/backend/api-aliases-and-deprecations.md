# API Aliases And Deprecations

New aliases:

- `/api/workflows/*`
- `/api/executions/*`
- `/api/decisions/*`

New read-model endpoints:

- `/api/agents/*`
- `/api/tasks/*`
- `/api/accounting/*`
- `/api/system-state/*`

Legacy builder-first routes remain active and emit deprecation headers during the migration window.
