# Summaries Layer

The summaries layer exists to keep the frontend from rebuilding organizational state from raw facts on every page.

Phase 1 responsibilities:

- agent registry sync
- task projection sync
- decision ledger sync
- accounting ledger and aggregate sync
- organization overview summary
