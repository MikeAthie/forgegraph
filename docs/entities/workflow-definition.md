# Workflow Definition

A workflow definition is the authored automation structure.

- Stored today as `Graph`
- Versioned through `GraphVersion`
- Remains important, but is not the primary product noun
- Lives in the secondary `Workflows` workspace

Operator views should reference the workflow definition as provenance, not as the main navigation frame.
