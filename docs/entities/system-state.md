# System State

System state is a time-scoped read model over:

- active agents
- active tasks
- pending decisions
- recent executions
- memory posture
- policy posture
- accounting posture

It is not a new execution source of truth. It is a projection layer over canonical runtime facts.
