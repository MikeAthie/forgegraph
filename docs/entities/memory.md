# Memory

Memory is the inspectable knowledge layer.

Canonical records remain:

- `MemoryObservation`
- `MemoryChunk`
- `MemorySession`

The OS frame treats memory as operational state:

- what was learned
- what influenced an execution
- what degraded
- when it changed
